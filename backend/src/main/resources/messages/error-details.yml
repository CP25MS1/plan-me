401:
  google:
    token:
      null: "Failed to retrieve ID token from Google"
      invalid: "Invalid ID token"

user:
  400:
    preference: "User preference must be TH or EN."

trip:
  400:
    name:
      null: "Objective name cannot be null."
    endDate:
      conflict: "End date cannot be before start date."
    badgeColor:
      null: "Objective badge color cannot be null."
      format: "Color format is invalid."

reservation:
  400:
    file:
      maxSize: "Total file size exceeds 20 MB."
      type: "Invalid file type. Supported file type are: .pdf, .jpg, .jpeg, .png."
    type:
      notEqual: "Each uploaded file must have a corresponding reservation type."
      invalid: "An unsupported reservation type was provided."
  500:
    file:
      ocrFailed: "The system could not read the content of the uploaded file."
      invalidOrNotMatched: "The uploaded file does not contain valid reservation information for the selected type or Selected reservation type does not match file content."
      mapFailed: "An error occurred while converting file content into reservation data."