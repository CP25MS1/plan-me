401:
  google:
    token:
      null: "Failed to retrieve ID token from Google"
      invalid: "Invalid ID token"

user:
  400:
    preference: "User preference must be TH or EN."

trip:
  400:
    name:
      null: "Objective name cannot be null."
    endDate:
      conflict: "End date cannot be before start date."
    badgeColor:
      null: "Objective badge color cannot be null."
      format: "Color format is invalid."

reservation:
  400:
    file:
      maxSize: "Total file size exceeds 20 MB."
      type: "Invalid file type. Supported file type are: .pdf, .jpg, .jpeg, .png."
    type:
      notEqual: "Each uploaded file must have a corresponding reservation type."
      invalid: "An unsupported reservation type was provided."
  500:
    file:
      ocrFailed: "The system could not read the content of the uploaded file."
      invalidOrNotMatched: "The uploaded file does not contain valid reservation information for the selected type or Selected reservation type does not match file content."
      mapFailed: "An error occurred while converting file content into reservation data."

album:
  400:
    name:
      invalid: "Album name is required and must be at most 100 characters."
    cursor:
      invalid: "The album pagination cursor is invalid."
    limit:
      invalid: "The requested album page size is out of allowed range."
  500:
    storage: "Storage operation failed while processing album."

memory:
  400:
    files:
      empty: "No memory file was provided."
    extension:
      invalid: "Unsupported memory extension was provided."
    file:
      maxSize: "A memory file exceeds the per-file size limit."
    request:
      maxSize: "Total upload payload exceeds the request size limit."
    tripQuotaExceeded: "Total memory size for this trip exceeds the configured quota."
    cursor:
      invalid: "The pagination cursor is invalid."
    limit:
      invalid: "The requested page size is out of allowed range."
    memoryIds:
      empty: "No memory ids were provided for bulk delete."
      invalid: "Memory ids must be positive integers."
  500:
    storage: "Storage operation failed while processing memory."

travelSegment:
  400:
    samePlaces: "Unable to create route as the starting and ending place are the same."
    unsupportedMode: "An unsupported mode was provided."
