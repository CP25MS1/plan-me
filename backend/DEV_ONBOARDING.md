# 📝 Backend Developer Onboarding (Spring Boot)

## 1. Local Setup

หลังจาก clone repo มาแล้วให้เปิด IntelliJ IDEA แล้วคลิก Open แล้วเปิด project ที่ clone มา โดยเลือก `/backend`

สร้าง .env file ที่ /backend แล้วขอจากทีม

ต่อมาเลือก `Current File` > `Edit Configurations...` ที่มุมขวาบนของหน้าต่าง IntelliJ IDEA
จากนั้นคลิกที่เครื่องหมายบวก (+) มุมซ้ายบนของหน้าต่างที่เด้งขึ้นมา แล้วเลือก `Spring Boot` จากนั้นตั้งค่าดังนี้:

- Name: `Run Backend`
- Main class: `capstone.ms.api.Cp25Ms1BackendApplication`
- SDK: Java 21 SDK
- Modify options > Environment variables > เลือกเป็น `{projectRoot}/backend/.env`

Apply แล้วกด OK

ต่อมาเป็นขั้นตอนเตรียม database ให้เปิดไปที่ `/infrastructure/database`

1. ขอ .env file จากทีม แล้วสร้างเป็น `/infrastructure/database/.env`
2. run คำสั่ง `docker compose up -d --build`
3. ตรวจสอบว่า container รันอยู่โดยใช้คำสั่ง `docker ps -a`
4. เข้าไปที่ `localhost:8080` จะเจอหน้า pgAdmin ให้ล็อกอินด้วย email และ password ที่ได้จาก .env file
5. เพิ่ม server ใหม่ใน pgAdmin โดยคลิกขวาที่ Servers > Register > Server...
   - General tab: ตั้งชื่อ server ตามที่ต้องการ
   - Connection tab:
     - Host name/address: `db`
     - Port: `5432`
     - Maintenance database: `postgresql`
     - Username: จาก .env file
     - Password: จาก .env file
   - กด Save

## 2. Project Structure
```
src
    └── main
        ├── java
        │   └── capstone.ms.api
        │       ├── common          # โค้ดที่ใช้ร่วมกันทั่วโปรเจค เช่น clients, config, properties, exceptions, utils
        │       ├── modules         # แยกตามฟีเจอร์หรือโดเมนต่าง ๆ ของแอปพลิเคชัน
        │       └── Cp25Ms1BackendApplication.java  # Main class สำหรับรันแอปพลิเคชัน Spring Boot
        └── resources
            ├── application.yml  # Configuration file ของ Spring Boot
```

## 3. Spring Boot Basics

- **Controllers**: จัดการ HTTP requests และ responses
- **Services**: ประมวลผลโลจิกของแอปพลิเคชัน
- **Repositories**: จัดการการเข้าถึงข้อมูลจากฐานข้อมูล
- **Entities**: แทนตารางในฐานข้อมูล
- **DTO (Data Transfer Object)**: ใช้สำหรับส่งข้อมูลระหว่างเลเยอร์ต่าง ๆ เช่น request และ response
- **Properties**: คลาสที่แมปกับ configuration properties จาก application.yml

### Files ที่สำคัญ:
- `JwtAuthFilter.java`: จัดการการตรวจสอบ JWT token สำหรับ authentication
- `SecurityConfig.java`: ตั้งค่าความปลอดภัยของแอปพลิเคชัน
- `GlobalExceptionHandler.java`: จัดการ exception ที่เกิดขึ้นทั่วแอปพลิเคชัน **(ยังไม่ได้ทำ)**
- `application.yml`: ไฟล์ configuration หลัก
