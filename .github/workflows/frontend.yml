name: Frontend Build & Push

on:
  pull_request:
    branches: [ main, uat, develop ]
    types: [ closed ]
    paths:
      - 'frontend/**'
  workflow_dispatch:

permissions:
  contents: read

env:
  DOCKER_NAMESPACE: cp25ms1
  SERVICE_DIR: frontend

jobs:
  build:
    if: >
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'pull_request' && github.event.pull_request.merged == true) ||
      (github.event_name == 'push' && github.event.head_commit != null && !startsWith(github.event.head_commit.message, 'Merge pull request'))
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Resolve branch -> env
        id: vars
        run: |
          BRANCH="${{ github.ref_name }}"
          case "$BRANCH" in
            main) ENV=prod; BASE_PATH=""; NEXT_PUBLIC_BACKEND_URL="http://localhost:12480/api" ;;
            uat)  ENV=uat;  BASE_PATH="/uat"; NEXT_PUBLIC_BACKEND_URL="http://localhost:11480/api" ;;
            develop) ENV=dev; BASE_PATH="/dev"; NEXT_PUBLIC_BACKEND_URL="http://localhost:10480/api" ;;
            *) ENV=dev; BASE_PATH="/dev"; NEXT_PUBLIC_BACKEND_URL="http://localhost:10480/api" ;;
          esac
          echo "ENV=$ENV" >> $GITHUB_OUTPUT
          echo "BASE_PATH=$BASE_PATH" >> $GITHUB_OUTPUT
          echo "NEXT_PUBLIC_BACKEND_URL=$NEXT_PUBLIC_BACKEND_URL" >> $GITHUB_OUTPUT

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push frontend image
        run: |
          IMAGE="${{ env.DOCKER_NAMESPACE }}/frontend:${{ steps.vars.outputs.ENV }}"

          docker build \
            --build-arg BASE_PATH="${{ steps.vars.outputs.BASE_PATH }}" \
            --build-arg NEXT_PUBLIC_BACKEND_URL="${{ steps.vars.outputs.NEXT_PUBLIC_BACKEND_URL }}" \
            -t "$IMAGE" ./frontend

          docker push "$IMAGE"
