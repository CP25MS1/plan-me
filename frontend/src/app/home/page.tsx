'use client';

import { Container, Box } from '@mui/material';

const HomePage = () => {
  return (
    <Container sx={{ paddingRight: '0rem' }}>
      <Box component="header" sx={{ paddingTop: '3rem', paddingRight: '1rem' }}></Box>
    </Container>
  );
};

export default HomePage;
